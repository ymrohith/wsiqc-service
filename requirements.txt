# Runtime
numpy>=1.26
Pillow>=10.0
tifffile>=2024.1.30
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2

# Development
pytest>=8.0
httpx>=0.27

# Optional: real pathology formats (.svs, .ndpi, .mrxs).
# Needs the OpenSlide C library installed separately:
#   conda install -c conda-forge openslide-python
#   apt-get install libopenslide0     (then: pip install openslide-python)
#   brew install openslide            (then: pip install openslide-python)
# The service runs without it -- pyramidal TIFF works out of the box.
# openslide-python>=1.3
